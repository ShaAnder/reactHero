// Math constants used across movement, angles, and thresholds.

export const HALF_PI = Math.PI / 2;
export const TWO_PI = Math.PI * 2;
export const EPSILON_DIST = 1e-3; // tiny distance to avoid divide-by-near-zero

/*
HOW THIS FILE WORKS
Small, shared math constants. 
*/
